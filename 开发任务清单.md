# sy-ftp — 开发任务清单（基于当前进度与代码风格）

说明：按优先级排列。已完成项标注 DONE。每项尽量包含验收条件。

## P0 — 核心骨架（必须最先完成）
1. MainWindow 布局 — DONE
   - 验收：工具栏/侧栏/内容/状态栏按卡片式布局渲染，编译通过
2. HostManager UI — DONE
   - 验收：主机列表、Tag 筛选、ContextMenu 可用
3. Host 编辑对话框
   - 验收：表单校验（必填项）且保存回配置模型

## P1 — 远程文件浏览
4. 文件列表视图
   - 验收：显示 Name/Size/LastModified，目录与文件图标区分
5. 连接与导航联动
   - 验收：选中主机 Connect 后加载根目录；断开时清空视图

## P2 — 文件传输
6. 拖拽上传 (Drag & Drop)
   - 验收：支持多文件/文件夹；上传进度可见；不阻塞 UI
7. 文件下载
   - 验收：右键下载弹窗选择路径；显示下载进度；大文件不阻塞

## P3 — 远程编辑（关键特性）
8. 远程编辑完整实现
   - 验收：本地编辑后保存能自动上传并在状态栏显示最后同步时间

## P4 — 体验与持久化
9. 窗口置顶开关
10. 应用配置持久化（AppConfig）
    - 验收：主机列表、窗口位置/大小、主题与置顶状态在重启后恢复

## P5 — 健壮性与错误处理
11. 异常处理与用户提示
    - 验收：连接失败/超时/传输失败弹出友好错误对话且无崩溃
12. 单元验证（手动测试清单）
    - 验收：完成一次端到端测试（连接→列文件→下载→远程编辑→上传）

## 执行建议
- 每次改动后执行：taskkill /F /IM sy-ftp.exe /T && dotnet build && dotnet run
- 小步快跑：每完成一项提交并在 PR 描述写明验收步骤
